# Render static site — connect repo or use Blueprint
services:
  - type: web
    name: hurt-me
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html